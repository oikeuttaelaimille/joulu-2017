{
  "name": "joulu",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.17.0",
    "jquery-validation": "^1.17.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-redux": "^5.0.6",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.17",
    "react-transition-group": "^2.2.1",
    "redux": "^3.7.2",
    "spinkit": "^1.2.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "aws --region eu-central-1 s3 sync build s3://my-s3-bucket --exclude=index.html --cache-control max-age=604800 && aws --region eu-central-1 s3 cp build/index.html s3://joulu.oikeuttaelaimille.fi --cache-control max-age=3600"
  },
  "devDependencies": {}
}
